# Bloquea el acceso HTTP directo a cualquier fichero de esta carpeta.
# Solo debe entrarse aquí a través de index.php (front controller).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
